#!/usr/bin/env php
<?php
declare( strict_types = 1 );

require_once __DIR__ . '/../bootstrap.php';

use Symfony\Component\Console\Application;
use Cyclopol\Command\DownloadSources;
use Cyclopol\Command\ArticlesFromSources;
use Cyclopol\Command\NameStreets;
use Cyclopol\Command\GeocodeAddresses;

$application = new Application();

$application->add( new DownloadSources( $entityManager ) );
$application->add( new ArticlesFromSources( $entityManager ) );
$application->add( new NameStreets( $entityManager ) );
$application->add( new GeocodeAddresses( $entityManager ) );

$application->run();
